/// text_set_id(instance)
/// @desc Sets the instance that calls for special text behavior.
/// @param {object} instance
/// @returns {void}

var _instance; _instance = argument0;

with (ctrl_text) text_id = _instance;
