/// player_state_template(phase)
/// @desc
/// @param {int} phase
/// @returns {bool}

var _phase; _phase = argument0;

switch (_phase)
{
    case STATE_START:
        break;

    case STATE_STEP:
        break;

    case STATE_FINISH:
        break;
}