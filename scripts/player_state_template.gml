/// player_state_template(phase)
//

switch (argument0)
{
    // Start:
    case STATE_START:
        break;

        // Step:
    case STATE_STEP:
        break;

        // Finish:
    case STATE_FINISH:
        break;
}
