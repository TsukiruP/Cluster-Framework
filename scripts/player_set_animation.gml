/// player_set_animation(animation, [start])
//

// Animation moment:
if (argument_count >= 2) {
    animation_moment = argument[1];
} else {
    animation_moment = 0;
}

// Animation target:
animation_target = argument[0];
