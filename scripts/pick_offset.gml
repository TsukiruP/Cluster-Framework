/// pick_offset(which, offset, ...options)
/// @desc  Returns one of the arguments depending on the first argument and the offset.
/// @param {int} which
/// @param {int} offset
/// @param {any} ...options

return argument[(argument0 - argument[1]) + 2];
