/// player_get_input(input, check)
// Returns the player's input in their input array.

var input_id, input_check;

// Initialize:
input_id = argument0;
input_check = argument1;

// Return:
return input_player[input_id, input_check];
